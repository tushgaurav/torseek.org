{
  "name": "torseek",
  "private": true,
  "scripts": {
    "dev": "bun run --filter '*' dev",
    "dev:api": "bun run --filter @torseek/api dev",
    "dev:web": "bun run --filter @torseek/web dev",
    "build": "bun run --filter '*' build",
    "typecheck": "bun run --filter '*' typecheck",
    "lint": "bun run --filter '*' lint"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
